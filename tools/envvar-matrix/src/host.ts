/** Matrix host adapter: re-export of the shared PPTB bridge. */
export { dataverse, powerplatform, getConnections, initTheme, inToolbox, notify, onConnectionChange, openText, saveText, type LiveConnection, type Target } from "../../_shared/host";
