/** Matrix host adapter: re-export of the shared PPTB bridge. */
export { dataverse, getConnections, initTheme, inToolbox, notify, onConnectionChange, openText, saveText, type LiveConnection, type Target } from "../../_shared/host";
